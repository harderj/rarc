// use crate::{
// 	constants::FLOATING_EPSILON,
// 	geom::arc::*,
// 	math::{angle_within, approximates},
// };

// #[test]
// fn test_radius() {
// 	let radius = ARC_1.radius();
// 	assert!(approximates(radius, 10.0, FLOATING_EPSILON));
// }
